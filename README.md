MVP:
- [ ] render an input for a user to paste a large block of code
- [ ] display the text below
- [ ] render a button for the user to start the burn
- [ ] render dropdown input for the user to choose the speed at which the text burns in terms of characters per minute
- [ ] the text disappears at the chosen charaters per minute
- [ ] there is a "Add [n] characters back" button that allows the user to add text back if needed

Stretch:
- [ ] make the "burning" look more realistic
- [ ] auto scroll the page so that the burning portion is always visible at the top of the page